<html lang="en">

  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Tech Blog</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>

  <body>
    <header class="display-flex justify-space-between align-center p-2">
      <h1>
        Tech Blog
      </h1>
      {{#if logged_in}}
        <nav>
          <button class="btn" id="home-nav">Home</button>
          <button class="btn" id="dashboard-nav">Dashboard</button>
          <button class="btn" id="logout-nav">Logout</button>
        </nav>
      {{else}}
        <nav>
          <button class="btn" id="home-nav">Home</button>
          <button class="btn" id="login-nav">Login</button>
        </nav>
      {{/if}}
    </header>
    <main class="container container-fluid mt-5" id="homeComments">
      {{{body}}}
    </main>
    <footer>

    </footer>

    {{#if logged_in}}
      <script src="/js/logout.js"></script>
    {{/if}}
    <script src="/js/navigation.js"></script>
  </body>

</html>